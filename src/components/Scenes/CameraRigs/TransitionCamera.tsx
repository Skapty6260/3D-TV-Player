// Set the opacity of forest location to 0, smoothly moving camera far away
// Then run the player camera, start dropping the tv and add slidein animation to the player location

const TransitionCamera = () => {
	return <div>TransitionCamera</div>
}

export default TransitionCamera
